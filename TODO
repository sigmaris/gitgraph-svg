* Display the current commit in toolbar (or title)
* Display images when they're clicked on in tree
* Handle changed images better - stop using filetable
* Handle diffs with two branches for merges
* When displaying a commit, scroll the tree to show the changed files
* Add preferences dialog of some kind